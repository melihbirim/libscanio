{
  "name": "libscanio",
  "version": "0.1.2",
  "description": "Scan huge CSV/NDJSON/JSON files without loading them into memory",
  "main": "index.js",
  "files": [
    "index.js",
    "binaries/"
  ],
  "scripts": {
    "build": "cd .. && zig build node -Doptimize=ReleaseFast && mkdir -p node/binaries && cp zig-out/lib/scanio.node node/binaries/$(node -p \"process.platform + '-' + process.arch\").node",
    "test": "node test/addon_test.js && node test/test.js"
  },
  "keywords": [
    "csv",
    "ndjson",
    "scan",
    "streaming"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/melihbirim/libscanio.git"
  },
  "publishConfig": {
    "access": "public",
    "provenance": true
  },
  "engines": {
    "node": ">=18"
  }
}
