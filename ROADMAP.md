# Roadmap

## Core principle

`scan()` is lazy and streaming by default. Never materialize the whole dataset unless explicitly asked (`.collect()` / equivalent).

## Milestones

- [x] **M1 — Scanner**: file source, CSV parser, `next()`, row streaming, constant-memory, Zig API. Originally mmap-based; switched to fixed-size chunked reads (`src/root.zig`) after measuring peak RSS against `cat` on a 417MB/1M-row/51-col file — mmap held 426MB resident (touching every byte to scan it faults in, and keeps resident, every page: peak RSS == file size by construction, regardless of the scanner logic on top), `cat` held 1.4MB (fixed-size read-and-discard loop). Chunked reads bring `Scanner` down into that same regime — bounded by chunk size, not file size, so a 10GB file costs the same working set as a 10MB one. Almost every row is still a zero-copy slice into the current chunk; only a line that straddles a chunk boundary gets assembled into a small reused scratch buffer instead. `count()`'s no-WHERE fast path was adapted to count newlines chunk-by-chunk (`Scanner.countRemaining()`) rather than over one large mapped slice — same zero-field-parse property, still bounded memory. NDJSON and JSON arrays got the same treatment shortly after — see M4's updated entry below.

  Chunk size swept, not guessed: 10 runs each, same 417MB/1M-row/51-col file.

  | chunk size | time avg | RSS avg |
  |---|---|---|
  | 64KB | 0.577s | 2.01MB |
  | **256KB (default)** | 0.563s | 2.20MB |
  | 1MB | 0.562s | 2.96MB |
  | 4MB | 0.565s | 6.13MB |
  | 16MB | 0.600s | 18.73MB |

  Time is flat (0.56-0.58s) from 64KB through 4MB — syscall count isn't the bottleneck anywhere in that range, even at 64KB (~6,500 `read()` calls for this file). RSS scales roughly linearly with chunk size, with no time payoff for going bigger. 16MB is a strict loss on both axes — the buffer itself gets big enough that first-touch cost starts to show. Landed on 256KB as the default: same time as 1MB, 26% less RSS, with enough margin over 64KB to avoid its slightly wider run-to-run variance. At the chosen default, versus the old mmap version: 427MB -> 2.2MB peak RSS, ~30% slower (0.43s -> 0.56s single-thread) — a real, named tradeoff made for the memory win, not hidden.

  Chunk size is configurable, not hardcoded: `Scanner.openWithOptions(allocator, path, .{ .chunk_size = N })` directly, or `Query.open(allocator, path, .{ .csv_chunk_size = N })` — `null` (the default either way) uses the measured 256KB default. Not exposed through the C ABI or Python binding yet (both use the default); add if a real caller needs to tune it, not speculatively.
- [x] **M2 — Scan operations**: projection, filter, limit, first, count. `count()` with no WHERE clause takes a zero-field-parse fast path (raw newline counting on the mapped bytes) — measured ~10x faster than a full row scan on a 500K-row file. `limit(N)` returns in time proportional to N, not file size (measured near-instant regardless of file size).
- [x] **M3 — C ABI**: `scanio_open` / `scanio_next` / `scanio_count` / `scanio_column_index` / `scanio_close` / `scanio_last_error`, minimal, no Zig internals exposed. Allocator is `std.heap.c_allocator`, not `GeneralPurposeAllocator` — this library gets `dlopen()`'d into a host process, and GPA's PageAllocator faults there (the exact bug csvql shipped once, #149). Verified for real: `zig build smoke-test` loads the built `.dylib`/`.so` via Python `ctypes` — the actual `dlopen()` path, not just Zig's in-process test runner — and runs 200 consecutive open/close cycles to catch allocator faults that only surface after repeated use.
- [x] **M4 — NDJSON, plus JSON arrays**: `src/ndjson.zig`, wired into `Query` via a `Source` tagged union so `scan("data.csv")` and `scan("data.ndjson")` (format inferred from extension, or explicit override) behave identically at every layer — filter, projection, limit, and the newline-only `count()` fast path all work the same for both, since both formats are one-record-per-line. `.json` files are also routed to the NDJSON path; the first non-whitespace byte (sniffed from the first chunk, see below) decides NDJSON-lines vs JSON-array. Nested objects/arrays *as field values* are still out of scope and fail loudly (`NestedValueNotSupported`) rather than silently stringifying.

  Originally mmap/full-load, same as CSV was before M1's fix, and for the same reason: measured 57.7MB peak RSS on a 56MB/1M-row NDJSON file and 51.1MB on an 11MB/200K-row JSON array file — both tracking file size, not row count. Rebuilt on the same chunked-read foundation as CSV (`NdjsonScanner` now has its own `buf`/`buf_len`/`buf_pos`/`line_scratch`, same shape as `Scanner`'s). NDJSON lines use the identical newline-boundary `nextLine()` CSV already has. JSON arrays needed a different boundary rule — no newlines to rely on inside `[{...},{...}]` — so `nextObject()` tracks brace depth and in-string/escape state across chunk refills instead, extracting one balanced `{...}` at a time; the old `json_array.zig::jsonArrayToNdjson()` (convert-the-whole-array-up-front) is no longer called by this path, kept only as a tested reference implementation. The header is derived from the first row's keys at `open()` time (same schema model as before) and that first row's text is cached and replayed as the first `next()` result, since a chunked reader can't rewind a file position the way a full mmap could.

  Measured, 10 runs each, old (mmap/full-load) vs new (chunked/streaming):

  | | time avg | RSS avg |
  |---|---|---|
  | NDJSON old, 56MB/1M rows | 0.176s | 57.72MB |
  | **NDJSON new** | 0.174s | **1.98MB** (29x less) |
  | JSON array old, 11MB/200K rows | 0.033s | 51.10MB |
  | **JSON array new** | 0.050s | **1.97MB** (26x less) |

  NDJSON's win is essentially free — same speed, 29x less memory, since line-splitting was never the bottleneck (JSON parsing dominates either way, unaffected by the I/O layer change — verified in isolation before trusting the headline number, since an earlier run of this same comparison showed a bogus 18x *slowdown* that turned out to be a stale, un-rebuilt benchmark binary, not a real regression). JSON array's win costs ~50% more time — the streaming brace-depth scan does genuinely more structural work per byte than one bulk convert pass followed by ordinary line-splitting did. Both are now bounded by chunk size regardless of file size, matching CSV and `cat`.

  Later, real correction: every NDJSON/JSON-array throughput number above (4.3-6.0M rows/sec) used a 3-4-field test fixture. Measured against a real 51-column NDJSON file (the same taxi CSV data used everywhere in this project, converted to NDJSON) and got 187K rows/sec — 23x slower for 17x more fields, the signature of O(n^2) cost. Root cause: `next()` looped over every header key and called `JsonObject.get()` (a linear scan over the row's own fields) once per key. Fixed with `header_index` (`src/ndjson.zig`) — a hash map from header key to column index built once at `open()` — turning the lookup into one pass over each row's own fields instead of one pass per header key. Isolated before AND after fixing it: parse-only (no lookup at all) measured 242K rows/sec on the same wide fixture, meaning the fixed lookup now costs ~13% overhead (210K vs 242K rows/sec) instead of being the dominant cost. Narrow fixtures unaffected (4.1-4.3M rows/sec, unchanged). The remaining 242K rows/sec ceiling on wide rows is JSON parsing itself (tokenizing, escaping, building `Field` structs — costs that scale with field count regardless of lookup strategy), a different, harder lever than this one was.

  Pushed on that lever, not just accepted it: isolated SIMD tokenizing alone (`findJsonStructure`, no field extraction) at 778K rows/sec on the same wide fixture — 3.2x above the 242K parse-only number, confirming (as the old tok_bench claim said) that tokenizing is NOT the bottleneck; per-field key/value extraction is. Read every function in that extraction path (`parseObjectReuse`, `parseValueAfterColon`, `findStringEndFromTokens`, `advanceTokenIndex`, `hasJsonEscape`) looking for another O(n^2)-class bug like the header one — found none: both key and value extraction are O(1) per field (monotonic token-index advance, zero-copy string slices for the unescaped case this fixture hits). Added a positional fast path anyway (`src/ndjson.zig` `next()`) — real NDJSON files usually keep stable key order row-to-row, so checking `obj.fields[k].key == header[k]` by `mem.eql` first, before falling back to `header_index.get()`, skips hashing entirely on the common case. Measured its effect honestly: 204K-234K rows/sec across repeated runs, overlapping the pre-change 210K within noise — kept the change (correct, harmless, directionally right for stable-order files) but did NOT report it as a proven win. That "not a remaining algorithmic bug" conclusion was wrong — it was scoped to the extraction loop, not the SIMD tokenizer itself, and the tokenizer had a real, fixable inefficiency: `findJsonStructureN` (`src/json_simd.zig`) found structural chars with a per-lane `if (!is_any[j]) continue` scalar loop over every lane of the SIMD chunk (16 bytes at a time), even though real JSON text is sparse in structural chars — most of a 51-field row is string/number content between them. That loop's branch is data-dependent on where structural chars fall, so it mispredicts constantly on real content, not the tidy `{"a":1,"b":2}` pattern short benchmarks tend to use. Fixed with the standard simdjson technique: convert the SIMD match vector into a bitmask (branchless — shift-and-or per lane, no data-dependent conditional) and walk it with `@ctz` + clear-lowest-set-bit, which visits exactly `popcount(mask)` positions instead of scanning all 16 lanes every chunk. Measured on the same wide (51-field) fixture, repeated 3x for stability (209K-234K noisy before vs 313K-315K tight after): full `next()` went from ~210-230K rows/sec to a consistent ~313-315K rows/sec — a real ~40-50% gain, not noise (variance itself dropped, a second signal it's a genuine fix, not a lucky run). JSON array format (shares the same tokenizer) went 132K → 167K rows/sec, ~26% up. Narrow (3-field) fixture unaffected/slightly up (4.99M rows/sec, matching the old 4.1-4.3M baseline) — confirms the fix helps proportionally more where structural-char density is lower (wide rows, long string values), exactly where the old per-lane scan cost the most. Full Zig/Python/Node test suites re-verified green after the change.

  Still not CSV speed (CSV: ~2.1-2.2M rows/sec on the same wide fixture; NDJSON at 313-315K was ~7x slower), and told so directly. The real remaining cost wasn't another bug in the generic pipeline — it was the generic pipeline itself: building a Token array, a Field-struct array, and tagged JsonValue unions for data this scanner only ever reads back as raw strings is pure overhead CSV's comma-split never pays. Added `NdjsonScanner.tryFastRow()` (`src/ndjson.zig`) — a fused, single-pass byte scanner straight into `field_buf`, the same shape as CSV's own line-splitting, used only when a row matches the fast shape (same key order as the header, no escapes, no nested values); falls back to the full generic parser — unchanged, still the correctness source of truth — the instant anything doesn't match, so correctness never depends on the fast path succeeding. First version (using json_parser's manual per-byte escape-tracking `findStringEnd`) reached ~440-460K rows/sec. Checked `zson` (this parser's own ancestor project, `~/code/birim.one.dev/zson`) for a faster single-row technique — found none; `zson`'s `json_parser.zig` is a strict subset of this one (no `parseObjectReuse` equivalent), and its actual speed lever is `parallel_ndjson.zig`, multi-threaded chunked processing, not a smarter single-row parse. Applied the one real single-row win available: replaced the manual escape-tracking string-end scan with vectorized quote search (`std.mem.indexOfScalarPos`, SIMD-backed) plus a cheap backward escape-check only on an actual hit (`findQuoteEnd` in `src/ndjson.zig`) — the string equivalent of CSV's own comma search. That took the fast path to a stable ~510-540K rows/sec (3x runs, tight variance) — ~65% up from the pre-fast-path 313K, ~2.5x up from where this investigation started (210K). Narrow (3-field) fixture, JSON array format, and all Zig/Python/Node (46/46) test suites re-verified after this change too.

  Honest ceiling assessment: closing the remaining ~4x gap to CSV's 2.1M would mean either (a) genuinely inherent JSON cost CSV doesn't have — pairing two quotes per field (key + value) versus CSV's one delimiter search, even at SIMD speed, is real extra work per field — or (b) matching zson's actual lever, multi-threaded chunked parsing, which is a materially bigger architectural change: it breaks the single-threaded, bounded-memory `next()`-generator contract every consumer (C ABI, Python, Node, MCP) currently relies on, and hasn't been attempted here. Not pursued without explicit direction, since it changes the API contract, not just the algorithm.

  External checkpoint, not just self-comparison: benchmarked two other JSON parsers on the identical wide fixture, same job (extract all 51 fields/row), to check whether "still not CSV speed" meant libscanio specifically was slow or JSON itself is: Zig stdlib's own `std.json.Scanner` (the lowest-level streaming API it ships, no tree-building) measured ~273-280K rows/sec — libscanio's fast path (~510-540K) is already ~2x that. Real simdjson (the actual C++ reference implementation, installed via `brew install simdjson`, used through its On-Demand API — same lazy per-field-extraction shape as libscanio's fast path, not full-DOM parsing) measured ~710-745K rows/sec, single-threaded, via a small harness compiled against `libsimdjson`. Even the industry-reference SIMD JSON parser doesn't get near CSV's 2.1-2.2M on this data — confirms the ~4x CSV gap is structural to JSON (two quote-pairs per field vs one delimiter), not a libscanio shortfall. libscanio sits within ~1.4x of simdjson itself.

  Tried to close more of that 1.4x gap: attempted simdjson's actual technique — fuse the quote-search and escape-detection into one scan (find next `"` or `\`, whichever comes first) instead of two separate passes (`findQuoteEnd` then a full re-scan via `hasJsonEscape`). Implemented as a hand-rolled fixed-16-byte-chunk SIMD scan (same bitmask+`@ctz` shape as the earlier `json_simd.zig` fix). Measured it: a real regression, ~431-462K, worse than the ~510-540K baseline it was meant to improve. Root cause, confirmed by reading `std.mem.indexOfScalarPos`'s actual implementation: Zig stdlib already uses a tiered SIMD strategy (full vector blocks sized via `std.simd.suggestVectorLength`, then half, then quarter, then a scalar tail) that's well-tuned for short slices — most field values in this fixture are under 16 bytes, so the hand-rolled version's fixed-chunk threshold pushed those straight to a slower two-condition (quote-or-backslash) scalar loop instead of getting any vectorization at all. Reverted (`git checkout -- src/ndjson.zig`), confirmed back to the known-good, faster, committed version. Lesson worth keeping: a hand-rolled fixed-chunk SIMD routine isn't automatically better than calling stdlib's primitive twice — check what the primitive actually does before assuming a "fused" rewrite wins.

  Where this landed, not chased further without explicit direction: closing the remaining gap to simdjson's actual number means porting its real two-stage architecture (one pass over the *entire* document computing structural-character and string-boundary bitmasks together via carry-less multiplication, then a lazy second pass over a compact structural index) — a genuine rewrite, not a per-field tweak. Current state: NDJSON wide-fixture throughput went 187K (session start) → 210K (O(n^2) header-lookup fix) → 313K (SIMD tokenizer bitmask fix) → 510-540K (fused fast-path row scanner + vectorized quote search), a ~2.9x total improvement, verified 2x faster than Zig's own stdlib JSON scanner and within ~1.4x of the C++ reference implementation. Full Zig/Python/Node (46/46) test suites green throughout.

  | file (real taxi data, same 1M rows) | format | size | rows/sec | peak RSS |
  |---|---|---|---|---|
  | sample.csv | CSV | 417MB | ~2.1-2.2M | ~2.2MB |
  | sample.ndjson (51 fields) | NDJSON | 1.47GB | ~205K | ~2.0-2.1MB |
  | sample_array.json (51 fields) | JSON array | 1.47GB | ~132K | ~2.0-2.1MB |
  | sample_narrow.ndjson (3 fields) | NDJSON | 70MB | ~4.1-4.3M | ~2.0MB |

  Memory stays flat across every format and field count — the chunked-read design applies uniformly, not just to CSV. Speed does not: JSON's per-row parsing cost (SIMD tokenizing, escaping, object construction) that CSV's flat field-split never pays, plus JSON's own verbosity (repeating every key on every row — the 51-field NDJSON file is 3.5x the CSV file's size for the same data) explain most of the CSV-vs-JSON gap; JSON array trails NDJSON further (~1.5x slower) from the streaming brace-depth scan's real structural cost.

  Three real findings from actually measuring this, not assuming:

  1. The first implementation used `std.json.parseFromSlice` per line and measured 42K rows/sec vs CSV's 17M — 400x slower, building a full parse tree per row is real cost. Fixed by pulling in `json_parser.zig`/`json_simd.zig` from [zson](https://github.com/melihbirim/zson) (MIT, same author, vendored into `src/`) — a SIMD-tokenized, zero-copy JSON line parser already solving exactly this problem.
  2. Swapping the parser alone did *not* fix it — throughput was still ~39K rows/sec. The actual bottleneck was `std.heap.GeneralPurposeAllocator`'s per-allocation tracking overhead, not the parsing algorithm; switching the allocator passed to `Query.open()` to `std.heap.c_allocator` took it to 2.78M rows/sec — a 66x difference from allocator choice alone, on the identical parser and file. The C ABI already used `c_allocator` for an unrelated reason (GPA also faults when `dlopen()`'d), so Python callers got the fast path automatically the whole time — only the raw Zig-API benchmarks were measuring the slow path.
  3. Even with `c_allocator`, `NdjsonScanner` was still allocating and freeing a fields array (plus any escaped strings) fresh every row — a malloc/free pair per row, where CSV's `Scanner` does zero allocations per row via a reused, grow-not-reallocate `field_buf`. Replaced that with a `std.heap.ArenaAllocator` reset (`.retain_capacity`) at the start of each `next()` instead of individually freeing the previous row's `JsonObject` — reuses the same block across rows rather than returning it and re-requesting it every time, and skips `JsonObject.deinit()`'s per-field cleanup traversal entirely (arena reclaims everything in one reset). Took it to **4.3M rows/sec**.

  Final numbers, 500K rows: CSV 19.7M rows/sec / 12.8MB peak RSS, NDJSON 4.3M rows/sec / 25.4MB peak RSS — about 4.5x slower and 2x more memory than CSV. Not zero-copy parity (JSON structurally needs a per-row data structure CSV's flat field-split never does), but no longer 400x off, no longer unbounded, and now within the same order of magnitude as CSV rather than two orders away. Peak memory unchanged by the arena change — it retains the high-water-mark block rather than shrinking, a deliberate speed-over-footprint tradeoff, still bounded rather than growing.

  4. Done next: added `parseObjectReuse()` to `json_parser.zig` — same parsing logic as `parseObject()`, but the caller supplies a `fields` array to `clearRetainingCapacity()` and refill instead of a fresh `std.ArrayList` + `toOwnedSlice()` per call, same discipline CSV's `field_buf` already uses. `NdjsonScanner` holds that array as a persisted field, grown once via a real allocator and reused for the scanner's whole lifetime — not the per-row arena, which would defeat the reuse. One real bug this surfaced, caught by this file's own test suite (`ndjson: escaped strings decode correctly`): `owned_strings` (tracking escaped-string allocations) can't be persisted the same way, because `parseValueAfterColon()` grows *that* list using whatever single allocator it's handed — which for the reuse path is the per-row arena — so its backing array is arena-scoped, and freeing it later via the scanner's real allocator (a different allocator than whatever actually backed it) segfaulted. Fixed by keeping `owned_strings` local to each `next()` call instead of a persisted field.

  Effect depends on call pattern, reported honestly rather than picking whichever number looks best: in a **cold, single-process run** (the shape of a one-shot CLI/script invocation — `mem_check` scans once and exits), throughput went from 4.3M to 4.7M rows/sec — a modest ~10%, since first-touch costs (page faults, cold caches) dominate a short-lived process regardless of whether the code reuses buffers. In a **warm, repeated-call pattern within the same process** (5 runs back to back, first discarded), it went from an unmeasured baseline to a **steady ~6.0M rows/sec** — the reuse benefit compounds across calls once the working set is warm. CSV's own steady-state also jumps with warm-cache repetition (cold ~20M, warm ~44M on this hardware), so the CSV-to-NDJSON ratio lands around 4-7x depending on which regime you're comparing, not a single fixed number.

  Remaining lever, not yet done: the fields array is reused, but each row's *values* still get individually decoded/copied when escaped; a fully zero-copy path (only ever copying bytes that genuinely need unescaping, never touching the allocator for unescaped strings/numbers at all — which the parser mostly already does, per-field, but the per-row bookkeeping around it still costs something) is the next place to look if this needs to go further.
- [x] **M5a — Python binding**: `libscanio.scan(path, columns=[...], where="...", limit=...)` — the original target API. WHERE is a simple string (`col OP val [AND col OP val ...]`, no OR — the C ABI only represents a flat AND-list, and nothing has needed more than that yet). Column names are resolved to indices via one throwaway `scanio_open()` before the real, options-bearing open (the C ABI has no header-only entry point; two mmaps of the same file, not two reads, so the OS page cache absorbs it). Building this binding is also what surfaced a real C ABI gap — no way to list header column names — closed by adding `scanio_n_columns`/`scanio_column_name` to M3's surface rather than working around it in Python. Verified via `zig build python-test`, the real dlopen() path via ctypes, not just Zig's in-process tests; caught one real bug this way (two new C ABI functions missing from the ctypes `argtypes`/`restype` declarations caused a segfault — ctypes defaults to a 32-bit `c_int` return type, which mangles a 64-bit pointer/size_t).

  Later addition — `col IN (a, b, c)`: still no general OR (the C ABI's predicate list is still flat-AND-only), but IN covers the one case that gap actually blocked — "any of these values" — without needing N separate scans unioned in Python. New `Op.in_list` in `query.zig` (a predicate carries `values: []const []const u8` instead of one `value` when this op is used), a new `SCANIO_IN` C ABI op plus `values`/`n_values` fields on `scanio_predicate_t`, and `col IN (...)` syntax in the Python WHERE-string parser. One real ctypes gotcha worth naming: the `values` pointer in a `CPredicate` points at a ctypes array (`c_char_p * N`) that Python doesn't automatically keep alive just because a `CPredicate` references it — `_parse_where()` now returns that array alongside the predicates list (as `keepalive`) and every caller holds it in scope through the `scanio_open()` call that actually reads it, otherwise it risks being garbage-collected before or during the C call. Verified end to end on the real 417MB taxi fixture: `cab_type IN (yellow, green)` returned exactly 1,000,000 — matching the file's total row count, confirmed correct (not a bug) by checking yellow (967,553) + green (32,447) sums to exactly that.
- [x] **M5b — Node binding**: `node/` — same shape as the Python binding (`scan`/`scanArray`/`schema`/`count`/`aggregate`/`topk`/`profile`), via [koffi](https://koffi.dev) (dynamic FFI, no native compilation step, same "works right after install" property `ctypes` gives the Python side). Node has no built-in FFI (unlike Bun's `bun:ffi`); the alternative was a native N-API C++ addon — zero runtime npm dependency, but needs a C++ toolchain or a prebuilt-binary-per-platform CI matrix, real setup cost not taken on for a first Node binding.

  Real question raised before building it: would an FFI-via-npm-package layer cost meaningfully more than Python's `ctypes` did? Measured directly, same rigor as the ctypes investigation — pure Zig (`examples/collect_bench.zig`, no JS at all) vs `scanArray()` through koffi, same file, same query: low-selectivity (26 matches) added ~30-40ms fixed overhead (0.12-0.14s -> 0.157-0.172s); high-selectivity (967,553 matches) scaled to 0.341s -> 0.656-0.849s, but that increase tracks almost exactly what Python's `ctypes` path showed for the identical query (0.687s) — meaning it's the cost of constructing ~2M JS objects, not a koffi-specific FFI tax. koffi isn't a special problem; object-materialization cost is a property of the host language, not the binding technology.

  Design carried over exactly from the Python binding, including the parts that mattered: `stop_after_column`/`max_column` bounding computed the same way (safe for `scan()`/`scanArray()` only when `columns` is explicit, always safe for `count()`/`aggregate()`, deliberately NOT applied to `topk()` since it returns full rows and bounding would silently truncate them); the same WHERE-string grammar (`col OP val [AND ...]`, `col IN (a, b, c)`); the same bulk-collect path (`scanArray()`, one `koffi.decode()` bulk buffer read instead of one FFI call per field per row) for when a caller wants the whole result materialized fast rather than streamed. One real memory-safety question this raised, given the Python binding's use-after-free (predicates freed while `scan()` still read them): tested directly whether koffi has the same problem — forced GC pressure with 500 throwaway `Buffer.alloc()`s between every `scanio_next()` call on a WHERE-filtered scan, verified the result stayed correct across 300 calls. koffi manages string-argument memory safely across calls (unlike whatever ctypes' array-copy path was doing); kept as a permanent regression test in `node/test/test.js` anyway, same reasoning as the Python side — verified once isn't the same guarantee as verified forever.

  Verified via `zig build node-test` (36 tests, real `dlopen()` through koffi, not a mock) and directly against the real 417MB taxi CSV (967,553-row filtered result, byte-for-byte correct) — on macOS and Linux. **Windows is a known gap, not silently missing**: `scanio_open()` crashes outright through koffi on Windows CI (zero output, exit code 5 — no JS stack trace, meaning it's a native crash inside koffi's FFI engine, not a normal JS exception) regardless of what's passed for the options argument. Traced to that exact call via added FFI-call logging (`LIBSCANIO_DEBUG=1`, `node/lib/loader.js`), then tried two different targeted fixes — a struct-layout-mismatch theory and a null-struct-pointer theory (`NO_OPTIONS` in `node/lib/index.js`) — neither changed the outcome. Everything else Windows-related verifies clean in CI (Zig's own tests, the C ABI's real `dlopen()` smoke test, the Python binding — all going through koffi's exact sibling problem class and none of them hit it), so this is narrowly a koffi-on-Windows issue, not a libscanio one, and not fixable by guessing further without a way to actually reproduce and step through it on Windows. `ci.yml` skips the Node test steps on Windows specifically (not the whole platform — Zig/C-ABI/Python still run and pass there) until this gets a real diagnosis, rather than either hiding the gap or blocking every other Windows verification on an unsolved koffi bug.
- [ ] **M6 — Rust**: `cargo add libscanio`, C ABI first, idiomatic wrapper after.
- [x] **M7a — Aggregates + Top-K**: `aggregate()` computes count/sum/min/max/avg over a column in one streaming pass regardless of how many of those five you want — a second pass costs the same as the first. `topK()` adapts csvql's proven `TopKHeap` (`src/fast_sort.zig`, O(N log K) min/max-heap), minus its radix-sort key machinery (not needed for numeric-only top-K at this scope). Real cost named, not hidden: top-K has to retain K rows across the pass, and `Row.fields` point into a reused per-row buffer — so a candidate has to be copied to survive being kept. Mitigated, not eliminated: a row is only copied when `wouldAccept()` says it's an actual heap candidate, not for every row scanned. Measured on 500K rows: `topK(10)` costs about the same as a raw full scan (0.0191s vs 0.0283s) — one pass, not a full sort. Group-by not done (M7b, not started — no consumer has needed it yet).
- [x] **M8 — Higher-level adapter**: `mcp/` — an MCP server exposing scan/schema/profile/count/aggregate/topk as agent-callable tools. No MCP or AI-specific logic in libscanio itself — this is a separate consumer package (own `pyproject.toml`, own venv), a thin pass-through to the Python binding.

  Two of those six tools needed new C ABI surface first — `aggregate()` and `topK()` were Zig-only, never exposed past the C boundary. Added `scanio_aggregate()` (extern struct return: count/sum/min/max/avg + a `has_values` flag, since ctypes has no natural optional-float) and `scanio_topk()`/`scanio_topk_next()`/`scanio_topk_close()` (same open/next/close shape as the row scanner, walking a pre-sorted heap one entry at a time). Both drain the same `Ctx`/`Query` that `scanio_count()` already does — no new open path, no duplicated predicate-parsing. `Entry` (topk.zig's internal heap-slot type) had to become `pub` and get re-exported from `root.zig` for `c_api.zig` to name its type directly, instead of the type-inference hack that was there first (`@TypeOf(...).Item` on a value that doesn't have one — caught by `zig build test` failing to compile, not by review).

  `profile()` lives entirely in the Python binding, not the C ABI — it's schema() + count() + one aggregate() call per column that *looks* numeric (heuristic: does the first non-empty sampled value parse as a float). Real cost named, not hidden: each numeric column costs its own full scan, since `aggregate()` is one pass per call, not one pass total across columns — fine for an occasional profile call on an agent's first look at a file, not something to put in a hot loop over a wide file (the 51-column taxi fixture has 34 numeric-looking columns, so `profile()` on it costs roughly 34 full scans).

  Verified three ways, not just imported: `zig build test` (new C ABI tests: aggregate values, aggregate-with-no-numeric-values sets `has_values=0`, topk ordering, topk composing with a WHERE-filtered ctx), `zig build python-test` (26 tests now, up from 9 — schema/count/aggregate/topk/profile added, all through the real `dlopen()` path), and the MCP server's tools called through the actual `mcp` SDK's `call_tool()` path (not a direct Python call) against both a small fixture and the real 417MB/51-column taxi CSV — confirmed correct results including `profile()`'s 34-numeric-column classification and per-column real aggregates on that file.

  Follow-up, once real workload comparisons surfaced it: `libscanio.scan(...)` materialized into a Python list (`list(scan(...))`) was measured *slower* than a naive `csv.DictReader` loop doing the same filter in pure Python — 22.5s vs 10.8s on the 417MB/1M-row fixture, unprojected. Root cause: `scanio_next()` is one FFI call per row, and each row needs one `.decode()` per field — ~2M individual Python/C boundary crossings for a ~967K-row result, and that per-call cost dominates once you want every matching row back as real Python objects, not just a count. The Zig-side scan+filter itself is fast (0.43-0.48s, matches the pure-Zig `filter_bench` number) — the cost is entirely in how the result crosses into Python one field at a time.

  Fixed with a new C ABI path — `scanio_collect()`/`scanio_collect_data()`/`_n_rows()`/`_n_cols()`/`_close()` — that runs the WHOLE filtered scan in one C call and packs every matching row's fields into one NUL-separated buffer, handed to Python as a single `ctypes.string_at()` bulk copy instead of one call per field. `libscanio.scan_array()` wraps this: one bulk `decode()` over the whole buffer (not one per field — measured as the single biggest win, cutting the Python-side reshape from ~0.9s to ~0.06s on its own) and `zip(*[iter(parts)] * n_cols)` to chunk the flat field list into rows (~3x faster than index-slicing `parts[i*n_cols:(i+1)*n_cols]` per row, measured). End state on the same file/query/2-column-projection: `scan_array()` at ~0.61-0.69s steady-state, down from `list(scan())`'s 4.76-4.80s projected / 22.5s unprojected — a real ~7x win, not quite matching the pure-Zig 0.43-0.48s floor (materializing ~967K real Python tuple objects has its own irreducible CPython cost) but close.

  Real bug this surfaced along the way, not in the new code but in the benchmarking process itself: `zig build test` or `zig build python-test` run WITHOUT `-Doptimize=ReleaseFast` silently rebuilds `zig-out/lib/libscanio.*` at Debug as a side effect (it's a dependency of both steps), overwriting a prior ReleaseFast build at the same path with no error — cost real time twice in this session, chasing a "why is this suddenly 7x slower" regression that was actually a stale/wrong-optimization-level `.dylib`, not a real code regression. Documented directly in `build.zig` next to the `c-lib` step now: always rebuild `c-lib -Doptimize=ReleaseFast` as the LAST command before benchmarking anything against the shared library.

  `scan_array()` is deliberately NOT memory-bounded the way `scan()` is — it materializes every matching row in both Zig and Python at once, same tradeoff any "give me an array" API has. Use `scan()`'s generator and don't collect a list if the actual goal is staying within bounded memory on a file too big to hold in RAM; use `scan_array()` when you know the result set is manageable and want it back fast as real Python objects.
- [x] **Bounded field-splitting (`stop_after_column`)**: `splitInto()` used to split every field of every row regardless of what the query actually reads — real cost, discovered by fairly comparing libscanio against xan (a real Rust CSV tool, same weight class) and finding libscanio lost by ~1.35-1.5x on low-selectivity queries despite winning on high-selectivity ones. Root cause, confirmed with a pure-Zig benchmark (`examples/collect_bench.zig`, no Python layer at all, to rule out ctypes overhead first) before touching anything: at low selectivity almost every row gets scanned and discarded, so the per-row scan cost dominates — and libscanio was scanning 51 fields to answer a question that only needed 2-6 of them.

  Fix: `ScannerOptions.stop_after_column` (new, `src/root.zig`) — `splitInto()` stops the instant it's captured the highest column index the caller will ever read, never scanning the rest of the line's bytes at all, not just discarding them. Threaded through `QueryOptions.stop_after_column` (`src/query.zig`), the C ABI's new `COptions.max_column` (`-1` = unbounded/old behavior, the safe default), and computed automatically in the Python binding: `scan()`/`scan_array()` bound it to `max(projected columns, WHERE columns)` whenever an explicit `columns` list is given (columns=None still means "need everything," correctly left unbounded); `count()`/`aggregate()` bound it to WHERE columns plus (for `aggregate()`) the analysis column, since neither ever returns row data so it's always safe. `topk()` deliberately was NOT given this treatment — it returns full-row dicts (every column, not just the sort column) as documented behavior, and bounding it would silently truncate that; revisit only alongside a real API contract change, not as a silent optimization.

  Real ctypes gotcha this surfaced: `COptions` gained a new field (`max_column`) but ctypes zero-initializes any Structure field not explicitly passed to the constructor — every single `COptions(...)` call site in the Python binding had to be updated to pass it explicitly, or it would have silently defaulted to `0` (meaning "only column 0 is ever needed"), a correctness bug far worse than no optimization at all. Caught before it shipped by grepping every construction site, not by a test that happened to notice.

  Measured, same 417MB/1M-row taxi file, `rate_code_id = 6` (26 matches, column 5 of 51 — an early/low-selectivity case): pure Zig 0.44s -> 0.13s (3.6x), and through Python `scan_array()` 0.45s -> 0.15s (3x) — beating xan (0.30s) by ~2x, reversing the earlier loss outright. Mid-row case (`payment_type = DIS`, column 20, 1,085 matches): 0.45s -> 0.27-0.29s, now roughly tied with xan (0.29-0.30s) instead of 1.5x behind. At 8.5GB scale (`trips.csv`, same filter, 13,711/20M matches): 9.76s -> 6.52-6.60s, now beating xan's 7.26s outright. Late-column case (`dropoff_boroct2010`, column 47 of 51) showed no improvement, as expected — almost the whole row still has to be split when the needed column is near the end, so there's nothing to skip. The win is real but bounded by how early the needed columns fall in the schema, not universal.

- [~] **M9 — Parallel scan**: started, on branch `m9-parallel-scan`, first slice landed. Earlier measurement against `cat` on a 1M-row/51-col 417MB CSV: `Scanner` ran 0.42s single-thread (~2.4M rows/sec, CPU-bound on one core). csvql does the same full-row scan in 0.16s, but only because it spreads work across ~7-8 cores (pinned to one thread it's 1.70s, ~4x *slower* than libscanio's single-thread `Scanner` — the per-core loop was already solid, the gap was entirely the missing parallel path). A quick attempt to close it by switching `splitInto`'s scalar delimiter scan to `std.mem.indexOfScalarPos` made things slower (0.42s -> 0.58s, fields here average ~8 bytes, below indexOfScalarPos's vectorization break-even) and was reverted — see `splitInto`'s doc comment in `src/root.zig`.

  Design decisions made explicitly before writing code (asked, not assumed, since both are real API-shape choices): (1) keep the bounded-memory guarantee M1 exists for — ruled out csvql's own `src/parallel_mmap.zig` approach (a real, working reference: mmaps the whole file, splits into N line-boundary-aligned byte ranges, one `std.Thread` per range) specifically because mmap-ing the whole file re-introduces the exact peak-RSS-equals-file-size problem M1 moved away from. (2) keep `Query.next()`'s single-threaded streaming contract completely untouched — parallel scan is a separate, explicitly-opted-into entry point, not a change to the generator every consumer (C ABI, Python, Node, MCP) already depends on; threading a per-row streaming API means either buffering full results up front (defeats streaming) or a cross-thread ordered merge queue (thread pool lifecycle now has to survive across `next()` calls, dlopen()-safety needs re-verifying for every binding) — out of scope for a first slice.

  `src/parallel.zig`: each worker thread does its own `WORKER_CHUNK_SIZE`-bounded `pread()` loop over a disjoint byte range of ONE shared `std.fs.File` handle — `pread()` (POSIX `pread(2)`; Windows `ReadFile` with an explicit `OVERLAPPED` offset) doesn't touch a shared file-position cursor the way `read()` does, so concurrent `pread()` on one handle is safe without needing a file open per thread. Peak RSS bounded by `WORKER_CHUNK_SIZE * num_threads`, not file size. Range boundaries aligned to the next newline via a small growing-window `pread()` (starts at 64KB, doubles only if a single line is pathologically longer — never reads the whole remaining file just to find one boundary).

  First slice: `parallelCountRows()` — the no-WHERE-clause row-count case specifically, chosen first because it needs zero field splitting (pure newline counting per range, embarrassingly parallel, no cross-thread state beyond one integer per worker) — proves the range-splitting/alignment logic is correct before anything harder (WHERE filtering or field access across threads) gets built on it. Verified two ways: against `Scanner.countRemaining()` (the known-correct single-thread path) on a real 200K-row multi-chunk-boundary fixture, exact match; and against real numbers on the 417MB/1M-row fixture — single-thread 5.56M rows/sec (0.180s) vs parallel (auto-detected 12 threads) 46.1M rows/sec (0.022s), an 8.3x speedup, exact same count both ways. (Both numbers are higher than the earlier 2.4M reference — that measurement predates this session's OS-file-cache being warm from repeated recent scans of the same file; still a fair same-machine, same-run comparison since both single- and multi-thread numbers were measured back-to-back on the same warm cache.) Full Zig/Python/Node/C-ABI test suites (unaffected, `parallel.zig` is purely additive) still green.

  Extended the same session to cover NDJSON and JSON arrays, plus correction tests that caught two real bugs before they shipped:

  Bug 1 — silent miscounting on JSON arrays: `parallelCountRows()` originally only knew how to count newlines, which is correct for CSV/NDJSON (one record per line) but silently wrong for a JSON array — a pretty-printed array has far MORE newlines than objects, a minified one has ZERO. Fixed with `sniffFormat()` (same first-non-whitespace-byte rule `NdjsonScanner.openWithChunkSize()` already uses, reimplemented over one small `pread()` instead of opening a full scanner) routing JSON arrays to `countJsonArrayObjects()` instead. That counter is deliberately sequential, not thread-split, and says so in its own doc comment: a JSON array's Nth top-level object boundary can only be found by tracking brace depth from the start of the file (unlike a newline, a `}` isn't self-describing at an arbitrary byte offset — depth-1-vs-nested depends on everything read before it). A correct parallel split would need a sequential pre-pass to find aligned boundaries, and that pre-pass already IS the count — so splitting further buys nothing for bare counting. It would start paying off for parallel WHERE-filtered/field-extraction work (the actually-expensive part, per the earlier NDJSON investigation), not implemented here. Caught by two new tests specifically constructed to fail loudly under the old newline-based logic: a pretty-printed 3-object array (13 lines, would have overcounted) and a minified 4-object one-line array (0 newlines, would have undercounted) — both pass now, and both would have failed hard against the original implementation if run against it.

  Bug 2 — off-by-one on every NDJSON file: `parallelCountRows()` unconditionally subtracted 1 from the line count, correct for CSV (whose first line is a column-name header, already consumed by `Scanner.open()` before `countRemaining()` is ever called) but wrong for NDJSON (no header line — every line is a real data row). Every NDJSON file undercounted by exactly one row. Caught immediately by a straightforward correctness test (3-line NDJSON fixture, expected 3, got 2) — exactly the kind of bug this project's "verify before trusting" discipline exists to catch, and did. Fixed by making `query.zig`'s `inferFormat()` (the same extension-based CSV-vs-NDJSON rule every other part of libscanio already uses) `pub` and reusing it here, rather than inventing a second, potentially-drifting format rule specific to `parallel.zig`.

  Both fixes verified against real fixtures, not just the small unit tests that caught them: a 100K-row NDJSON file (matches `NdjsonScanner.countRemaining()` exactly, both single- and multi-thread) and the same 1M-object wide JSON array fixture used throughout the earlier NDJSON perf investigation (`/tmp/sample_array.json` — 1.47GB) — `parallelCountRows()` and `NdjsonScanner.countRemaining()` agree exactly (1,000,000). Full Zig (78 tests)/Python (45)/Node (46)/C-ABI (10 smoke) suites re-verified green after both fixes.

  WHERE-filtered parallel count landed next, same session: `parallelCountRowsWhere()` — CSV via a plain comma-split worker, NDJSON via `json_parser.parseObject()` (the simple, correct, allocating parser — deliberately NOT `ndjson.zig`'s optimized reuse/fast-path machinery, correctness first, revisit for speed later if this turns out parse-bound the way single-threaded NDJSON was) mapped into a `Row` by a `header_index` built once (from the file's first line) and shared read-only across every worker thread. `query.zig`'s `matches()` made `pub` and reused as-is, so filtering logic isn't duplicated between the streaming and parallel paths. JSON arrays return `FilterError.JsonArrayFilterNotSupported` explicitly rather than a silently wrong count — filtering one in parallel would need per-object parsing composed with `countJsonArrayObjects()`'s sequential brace-walk, not implemented here.

  Correction tests caught a THIRD real bug — worth naming since it's a genuinely easy trap: `buildNdjsonHeader()`'s first line was read into a stack-local `var buf: [WORKER_CHUNK_SIZE]u8`, and `json_parser.parseObject()`'s zero-copy fast path (the same one that makes NDJSON parsing fast throughout this session — see M4's entry) returns string slices pointing INTO WHATEVER BUFFER IT WAS GIVEN. Once `buildNdjsonHeader()` returned, that stack frame was gone — `header_index`'s keys were dangling pointers into reused stack memory. Every `header_index.get()` lookup then silently failed, every NDJSON field defaulted to `""`, and every WHERE clause evaluated false: a filtered parallel count of 0 on the real 1M-row wide fixture, where `Query.count()` (single-threaded) correctly found 1,000,000 matches. NOT caught by the small hand-written unit tests — small test fixtures happened not to trigger the specific stack-reuse pattern that made it visible; only caught by running the SAME real fixture this whole session has used for NDJSON perf work through both paths and diffing the counts. Fixed with one line: `try aa.dupe(u8, ...)` before parsing, so the header's keys live in the same arena as the `HashMap` that indexes them, not a function's stack frame. This is the second time this session a bug was invisible at small scale and only surfaced against real data (the first: the O(n^2) header-lookup bug in M4) — worth internalizing as a pattern for this codebase specifically: bounded-memory, chunked, multi-buffer code has failure modes (stale slices into reused buffers, quadratic costs) that only show up at real scale, not toy fixtures.

  Verified on the same real 1M-row fixtures used throughout M4 and M9: CSV filtered count matched `Query.count()` exactly (1,000,000), 9.8x speedup (0.610s single-thread -> 0.062s parallel, 12 threads). NDJSON filtered count also matched exactly (1,000,000 — after the dupe fix; 0 before it, per the bug above), 4.65x speedup (1.963s -> 0.422s; smaller than CSV's because the correctness-first `json_parser.parseObject()` path per row costs more than CSV's plain comma-split, consistent with M4's whole finding that NDJSON parsing is inherently pricier than CSV's). Full Zig/Python/Node/C-ABI suites re-verified green.

  JSON-array WHERE filtering closed the same session: `parallelCountRowsWhere()` now delegates JSON arrays to the existing single-threaded `Query`/`NdjsonScanner` path instead of returning an error — not a missing feature so much as a deliberate non-duplication. `countJsonArrayObjects()`'s own doc comment already established WHY: a JSON array's Nth object boundary can only be found by walking brace depth from the start of the file, so a correct parallel split needs a sequential pre-pass that costs as much as the filtering itself — reimplementing JSON-object parsing a THIRD time in this file (there's already `ndjson.zig`'s and this file's own NDJSON worker) for zero speed benefit wasn't worth it. Verified against `Query.count()` on the real 1M-object wide fixture (exact match, 1,000,000) and a pretty-printed multi-line array (2/3 objects match a city filter, correct either way the array is formatted).

  Parallel materialized row scan landed same session: `parallelScan()` — `scanArray()`'s equivalent (Python/Node bindings), brought into Zig core rather than left binding-only. Row order decided explicitly before writing code (asked, not assumed): workers' matches concatenated in RANGE order (worker 0's, then worker 1's, ...), not a promised global file-order merge — most bulk-scan callers don't need exact order and enforcing it would cost a real merge pass for no benefit to those that don't. Reuses the same CSV-comma-split/NDJSON-json_parser/JSON-array-delegates-to-Query split as `parallelCountRowsWhere()`, and `topk.zig`'s `OwnedRow`/`copyRow` (third reuse this session, still zero duplicated row-copy logic). Verified: exact row content (not just count) on a real 100K-row multi-chunk CSV file and a 50K-row NDJSON file, checking every returned row's OWN fields are mutually consistent (`amount == id*3`, `city == "Austin"`) — catches a wrong field/header mapping even when the aggregate count happens to be right, the same class of bug the header off-by-one and dangling-slice bugs earlier in this milestone were. Confirmed correct at the real 1M-row scale too (`/tmp/sample.csv`/`.ndjson`, matching `Query.count()` exactly). Full Zig/Python/Node/C-ABI suites green.

  Real external benchmark, different session: `~/code/sieswi/fixtures/ecommerce_10gb.csv` — a genuinely real 10.46GB/130M-row fixture found on disk (not generated for this) — run against duckdb, xan (Rust, github.com/medialab/xan), qsv, and `cat`/`grep`, both a plain row count and a `status='pending'` WHERE-filtered count. Correctness cross-checked first: every tool agreed exactly (130,000,000 total rows, 25,996,080 matches). `parallelCountRows`/`parallelCountRowsWhere` beat every other tool on both tasks — 1.91s / 3.37s vs duckdb's 5.28s / 5.58s (duckdb: multi-threaded, ~229/199MB peak) — while using ~4.7-4.9MB peak, ~45-50x less memory than duckdb. `examples/parallel_bench.zig` added as a permanent, reusable comparison tool (`zig build parallel-bench`), not a throwaway script.

  That same benchmark surfaced a real single-thread gap and led to a genuine fix: `Scanner.countRemaining()`'s newline count (root.zig) used a naive scalar per-byte loop — the exact same missed-SIMD class of bug the JSON tokenizer had earlier this session (see M4's entry). First comparison attempt was actually apples-to-oranges (worth naming as a lesson, not hiding): `mem_check.zig` — the existing example used for the initial single-thread numbers — calls `Query.next()` in a loop, which splits every field of every row; xan's/duckdb's plain `count` don't parse fields at all. Caught by checking the actual code path being measured, not just trusting a plausible-looking number. Added `examples/count_bench.zig`, which calls `Query.count()`'s real no-WHERE fast path, for a fair comparison. Fixed the scalar loop itself with `src/simd_count.zig`'s `countByte()` — a vectorized per-chunk popcount (`std.simd.suggestVectorLength`-sized vector, `@select`+`@reduce(.Add, ...)`), simpler than `json_simd.zig`'s bitmask+`@ctz` fix since a pure count needs no positions, just a sum. Applied everywhere the same scalar pattern existed: `Scanner.countRemaining()` (root.zig), `NdjsonScanner.countRemaining()`'s line-delimited branch (ndjson.zig), and `parallel.zig`'s `countRange()` worker. Result, same real 10.46GB file, single-threaded: `count_bench` (correct fast path) went from the mismeasured 13.4-13.6s down to **2.9-3.4s** — faster than both xan (6.00s) and duckdb (5.28s), single-threaded, on identical hardware. Peak memory unchanged (~1.4MB, still bounded by chunk size not file size). Full Zig (new `countByte` fuzz-style tests against a scalar reference, several buffer sizes straddling vector-width boundaries) /Python/Node/C-ABI suites green.

  Direct request: compare the parallel WHERE-filtered path against csvql (this project's own author's other CLI tool, real and fast — see this file's earlier M9 note on it) on the same real file, expecting the parallel path to win. First measurement didn't: csvql's `SELECT COUNT(*) FROM '...' WHERE status='pending'` ran 1.99s; `parallelCountRowsWhere()` ran 3.37s — slower, not faster. Real, findable cause, not a fundamental limit: `CsvFilterCtx.onLine()` (parallel.zig) split every column of every row (10, on this fixture) even though the WHERE clause only ever reads one (`status`, column 7) — the exact same `stop_after_column` optimization `root.zig`'s single-thread `Scanner` already had (see M-something's original note on it) was never applied to the parallel worker. Fixed with `maxPredicateColumn()` — computed once from the predicate list, threaded through `CsvFilterWorker`/`CsvFilterCtx`, same "stop splitting once every predicate's column is captured" rule `splitInto()` already uses. That alone brought filtered count to 2.21s (real ~35% win, not yet ahead of csvql). Second lever, also measured before assuming: `WORKER_CHUNK_SIZE` (256KB, root.zig's single-thread-tuned default, blindly reused here) — bumped to 1MB, filtered count settled at a stable ~1.86-1.88s across repeated runs, edging ahead of csvql's 1.99s; 2MB measured faster still (~1.8-2.0s) but pushed peak memory to ~27MB, within noise of csvql's own 29.3MB — kept 1MB as the better tradeoff (peak ~14.4MB, still ~2x below csvql) rather than chase the last bit of speed at the cost of the memory story this whole path exists for. Final numbers, same real 10.46GB/130M-row file: unfiltered count 2.04s/14.1MB (still ahead of csvql's 2.39s/29.3MB from the earlier comparison), filtered count 1.84-1.88s/14.4-14.5MB (now ahead of csvql's 1.99s/29.3MB on both axes). Full Zig/Python/Node/C-ABI suites re-verified green after both fixes.

- [x] **M10 — ORDER BY**: `src/order.zig`, single column, v1 (multi-column tie-breaking deliberately not attempted — small additive change whenever a real caller needs it, not before). Design decided explicitly before writing code (asked, not assumed): materialize-then-sort, not a bounded/external merge-sort — same tradeoff `aggregate()`/`topK()` already accept, peak memory scales with the FILTERED row count (post-WHERE), not the file size. `orderBy(allocator, *Query, column, descending)` drains an already-open `Query` (so WHERE/projection/limit all compose for free, no separate code path) into owned rows (`topk.zig`'s `OwnedRow`/`copyRow`, made `pub` and reused rather than duplicated) and sorts once with `std.mem.sort`. Comparator is numeric-first, string-fallback — the SAME rule `query.zig`'s `evalOne()` already uses for predicates, kept consistent on purpose rather than inventing a second ordering rule (`"10"` sorts after `"9"`, not before it the way a pure byte compare would put it). Verified: ascending/descending, numeric-vs-lexicographic ordering specifically (`"9"` before `"10"`), string columns, composition with WHERE, NDJSON parity with CSV, and the empty-result-set case.

  C ABI (`scanio_order_by`/`scanio_order_by_next`/`scanio_order_by_close`, same open/next/close shape as `scanio_topk`, minus the sort-key output topK has and ORDER BY doesn't) plus Python (`order_by()`) and Node (`orderBy()`) bindings added the same session — both wired through the real `dlopen()`/koffi path, not just imported. 49/49 Python, 50/50 Node.

  Verified against an independent reference, not just internal self-consistency: DuckDB (CLI, already installed) run against the SAME real 1M-row wide fixtures used throughout this session's NDJSON/M9 work — `SELECT trip_id FROM read_csv_auto('/tmp/sample.csv') WHERE fare_amount > 50 ORDER BY fare_amount ASC` (7,535 rows) compared against `orderBy()`'s own output on both `/tmp/sample.csv` AND `/tmp/sample.ndjson` (JSON values are all strings in this fixture, needed an explicit `CAST(... AS DOUBLE)` for DuckDB's `read_json_auto` to sort numerically — a real gotcha, not a libscanio issue). Exact-set match both ways (sorted `trip_id` lists identical, 7,535/7,535) plus a direct monotonicity check on the returned `fare_amount` sequence (not just trusting the count). Cross-checking DuckDB's own CSV-scan output against its own NDJSON-scan output was tried first and produced a large diff — turned out to be DuckDB's OWN tie-breaking non-determinism between two independent scans (many rows share a `fare_amount` value in this fixture), not a real signal; dropped that comparison in favor of the exact-set + monotonicity checks above, which aren't sensitive to tie order.

- [x] **M11 — describe() (per-column type inference)**: `describe(path, sample_size=1000)` — column names + an inferred type per column (`integer`/`float`/`boolean`/`datetime`/`string`/`empty`), sampled from the first `sample_size` rows. Design decided explicitly before writing code (asked, not assumed): (1) bounded sample, not a full column scan — same tradeoff `profile()`'s existing numeric-column heuristic already accepts, cost stays flat regardless of file size; (2) datetime detection included, but conservatively — a small set of unambiguous ISO-ish formats only (`YYYY-MM-DD[THH:MM:SS[Z]]` / `YYYY-MM-DD HH:MM:SS`), no MM/DD-vs-DD/MM guessing and no locale-dependent month names, since a wrong "string" classification is a missed nicety but a wrong "datetime" one is actively misleading. Classification requires EVERY non-empty sampled value to agree (checked boolean → integer → float → datetime → string, most specific first) — a bounded sample is already forgiving of rare exceptions further down the file, so requiring full agreement within the sample keeps false positives low.

  Python-composed, not Zig-core — same precedent `profile()` already set (built from `schema()`/`scan()`, no new C ABI surface needed for a sampled heuristic like this). Node binding (`describe()`) ported with matching logic (regex-based ISO pattern checks instead of `strptime`, same conservative rule). MCP tool added, verified through the real `call_tool()` path, not just imported.

  Verified against two independent real datasets already used throughout this session, not synthetic data invented for this feature: `~/code/sieswi/fixtures/ecommerce_10gb.csv` (alphanumeric `order_id`/`user_id`/`product_id` correctly stay `string`, not falsely `integer`; `quantity`/`price_minor`/`discount_minor`/`total_minor` correctly `integer`; `created_at` — `"2023-05-26T22:00:00Z"` — correctly `datetime`) and `/tmp/sample.csv` (the wide taxi fixture — `trip_id` correctly `integer`, `pickup_datetime`/`dropoff_datetime` correctly `datetime`, `pickup_longitude`/`fare_amount`/etc. correctly `float`) — results consistent with DuckDB's own independently-inferred column types on the identical files (`bigint`→`integer`, `timestamp`→`datetime`, `double`→`float`), confirming the heuristic isn't just self-consistent but actually right. Dedicated small fixtures added to both Python (52/52 passing) and Node (53/53 passing) test suites covering every type category (`integer`/`float`/`boolean`/`datetime`/`empty`) plus the alphanumeric-ID-stays-string case specifically, not just the types the real fixtures happened to exercise.

**M9 follow-up — real JSON-array parallel WHERE filtering, closing the gap M9's own entry left open.** `parallelCountRowsWhere()` previously delegated JSON arrays to the single-threaded `Query` path, reasoned as a deliberate non-duplication since a bare count's boundary walk already costs what the count does. Direct request to close that gap anyway, on the theory that WHERE-filtered work (real per-object JSON parsing + predicate matching, the expensive part) genuinely benefits from parallelism even though a bare count doesn't. Implemented: `findJsonArrayRanges()` — one sequential pass (same brace-depth/in-string/escape state machine as `countJsonArrayObjects()`, refactored into a shared `walkJsonArrayObjectCloses()` so both use one copy of the logic) records up to `num_threads` object-boundary-aligned byte ranges; `forEachJsonObjectInRange()` walks complete objects within one range (same chunk-boundary-carry shape as `forEachLineInRange()`, tracking a balanced `{...}` instead of a `\n`); `buildJsonArrayHeader()` derives the header from the first top-level object instead of a first line; `JsonArrayFilterWorker`/`JsonArrayFilterCtx` parse+match per object per thread, same shape as the NDJSON filter worker.

Verified correct across explicit thread counts (1, 2, 3, 7, 16 — including odd splits that don't evenly divide the object count) on the real 1M-object wide fixture (`/tmp/sample_array.json`, 1.47GB), every count matching `Query.count()` exactly.

Two real, logged, reverted attempts at speeding up the sequential boundary walk itself — worth keeping the record, not just the final working version: (1) A fixed-16-byte-chunk bitmask+`@ctz` scan (the exact technique that fixed `json_simd.zig`'s tokenizer earlier this session) was tried first and REGRESSED (3.06s → 4.10s on the real fixture) — structural chars in real JSON objects land roughly every 10-20 bytes, so most searches resolve inside the first 16-byte chunk, paying fixed vector setup cost without ever reaching a span where wider scanning helps; the same failure mode an earlier `ndjson.zig` fast-path attempt hit. (2) Switching to 4 separate `std.mem.indexOfScalarPos` calls (stdlib's tiered SIMD, which DID win for that earlier `ndjson.zig` case) was worse still — a genuine quadratic blowup, not just slower: the fixture has zero backslashes anywhere, so a `\` search from any position scans to the end of the current ~1MB buffer EVERY time it's called, and it's called on every structural hit (~every 15 bytes) — roughly `(buffer_size / 15)` full-buffer scans per buffer, which never finished inside a 60-second timeout. Reverted both times back to the plain scalar per-byte scan, which measured a real, stable ~20% speedup on its own (single-thread 4.60-4.73s → parallel 3.77-3.80s) — smaller than CSV/NDJSON's parallel wins, consistent with the boundary walk itself remaining a real, unavoidable sequential cost, but a genuine improvement over the single-threaded-delegation version M9 originally shipped. Full Zig/Python/Node/C-ABI suites re-verified green.

## V1 definition of done

```js
for await (const row of scan("50gb.csv", {
  columns: ["customer_id", "revenue"],
  where: "revenue > 1000",
  limit: 100
})) {
  process(row);
}
```

With: bounded memory, fast first-row latency, correct CSV parsing, Linux/macOS/Windows support, Zig API, C ABI, Node package, Python package. Everything else is secondary.

## Non-goals

Do not build: a database, a dataframe, a SQL parser, a query optimizer, distributed execution, an arbitrary-nested-JSON query engine, dataframe transformations, visualization, an AI agent, a GUI, or any mutation API (no update/delete/in-place edits/transactions — open, scan, close only).

## Benchmarking discipline

Correctness must be verified before any performance claim. Datasets: 10 MB / 100 MB / 1 GB / 10 GB. Scenarios: full scan, single-column projection, multi-column projection, filter at 50%/1% selectivity, `LIMIT 10`, `COUNT`, `SUM`, `TOP 10`. Measure rows/sec, MB/sec, memory, and time-to-first-row — not just throughput.

**Real external comparison: Node's own JSON stack, same task (count `trip_id > 500000`) on the real taxi-fixture JSON array.** At a 140MB/100K-object subset (small enough that Node's native JSON.parse can actually run it): libscanio 0.36-0.38s/16.8MB, V8's native `JSON.parse` 0.59s/715MB, the `simdjson` npm binding (real C++ simdjson via N-API) 3.34s/1.65GB — libscanio faster AND far leaner than both. At the real 1.47GB/1M-object scale, native `JSON.parse` doesn't just lose, it can't run at all — `ERR_STRING_TOO_LONG` (V8's string-length cap), after already burning 3.6GB attempting to read the file into one string.

Two findings worth keeping, not just the numbers: (1) full materialization is the actual cost driver, not parsing speed — Node's JSON tools all load the whole file into one string then build a JS object per record, so memory scales with file size regardless of how fast the byte-level parsing is, and hits a hard wall well under 2GB. libscanio never does either (chunked reads, C-land row buffers, only matched rows cross into consumer memory) — that's why it wins on speed too, not just memory. (2) A fast native library doesn't imply a fast binding — `simdjson` wraps the fastest JSON parser that exists and still lost to plain `JSON.parse` by ~6x, because the N-API marshaling boundary plus still-necessary JS object construction ate the SIMD advantage entirely. Same root cause, different ecosystem, as this session's own koffi-vs-ctypes findings on the Windows Node work (M5b) — the binding layer, not the parser, was the bottleneck both times.

## N-way concurrency experiment (per-query memory under concurrent load)

Motivated by an outside-eye question ("what's the actual research angle here, not just 'fast CSV parser'?"): every benchmark in this file up to now measured ONE query at a time — the traditional single-analyst-one-big-query shape every CSV/DB benchmark is built around. Agent tool-calling is structurally different: many independent, small, frequent queries, often concurrent (a fleet of agents, parallel subagent tool calls, many sandboxed sessions sharing one host). Peak memory for ONE query doesn't answer "how many of these can run at once on a memory-constrained host" — that's a different, unmeasured axis. Real experiment, not assumed: launch N independent, single-threaded processes simultaneously (each unaware of the others, `threads=1` on both sides to isolate memory scaling from CPU-parallelism confounds), measure peak COMBINED RSS across all N plus total wall time, N = 1/2/4/8/16/32. Real 500MB slice of the real `~/code/sieswi/fixtures/ecommerce_10gb.csv` fixture (`/tmp/bench_500mb.csv`, 6.22M rows, warm page cache both sides). Correctness verified first and under real concurrency (`matches=1244118` from every process, every N, both tasks).

**Task 1 — bare WHERE-filtered count** (`libscanio`'s `filter_bench`, single-threaded, vs `duckdb -c "PRAGMA threads=1; SELECT count(*) ... WHERE ..."`):

| N | libscanio wall / combined RSS | duckdb wall / combined RSS |
|---|---|---|
| 1 | 0.70s / 1.8MB | 2.37s / 86.8MB |
| 2 | 0.64s / 3.8MB | 2.30s / 173.9MB |
| 4 | 0.68s / 8.3MB | 2.44s / 347.8MB |
| 8 | 0.71s / 15.5MB | 2.82s / 654.5MB |
| 16 | 1.37s / 31.1MB | 4.75s / 1,300.2MB |
| 32 | 2.65s / 65.0MB | 10.09s / 2,470.4MB |

Both scale ~linearly in memory (expected — N independent processes), but the per-process constant differs by ~48x (libscanio ~2MB/query even under load, duckdb ~87MB/query even at `threads=1`). At N=32: 65MB total vs 2.47GB — 38x. Wall time degrades worse for duckdb too (4.3x slowdown N=1→32) than libscanio (3.8x) — and libscanio's baseline was already 3.4x faster, so the absolute gap widens sharply under concurrent load, not just the memory gap.

**Task 2 — materialized filtered scan** (`libscanio`'s `scan_bench.zig`, new: single-threaded, duplicates every matching row's fields into memory — same cost class as `scan_array()`/`scanArray()`, NOT the bounded-memory `count()`/streaming `scan()` path — vs `duckdb -c "PRAGMA threads=1; COPY (SELECT * ... WHERE ...) TO '/dev/null' (FORMAT csv)"`, duckdb's own natural way to produce all matching rows: a streaming write, not full in-memory materialization):

| N | libscanio wall / combined RSS | duckdb wall / combined RSS |
|---|---|---|
| 1 | 0.82s / 276.1MB | 4.50s / 88.6MB |
| 2 | 0.91s / 367.0MB | 4.37s / 113.2MB |
| 4 | 1.17s / 722.7MB | 4.39s / 225.9MB |
| 8 | 1.30s / 1,057.2MB | 4.41s / 445.7MB |
| 16 | 2.17s / 1,944.2MB | 4.48s / 879.9MB |
| 32 | 4.90s / 2,756.0MB | 4.66s / 1,761.9MB |

**This one reverses**, and it's the more important finding of the two, not a footnote: at N=32, libscanio's combined memory (2.76GB) is now HIGHER than duckdb's (1.76GB), and libscanio's wall time degrades badly under load (0.82s → 4.90s, ~6x) while duckdb's stays almost flat (4.50s → 4.66s). Root cause, consistent with what this codebase's own doc comments already say (`scanArray()`'s doc comment in `node/lib/index.js`: "not memory-bounded"): materializing EVERY matching row means duplicating it past the reused scan buffer — real, unavoidable memory cost that scales with (matches × N), same as any engine would pay. DuckDB's vectorized engine can stream matching rows straight to output without holding the whole result set in memory at once — a genuine architectural advantage for bulk output that libscanio's simpler row-at-a-time-then-copy model doesn't have. Also single-threaded DuckDB's near-flat wall time under N=32 concurrent COPY-to-null suggests its buffer/execution machinery amortizes far better under load than libscanio's per-process arena/copy overhead does — a real, not-yet-understood gap worth its own investigation, not just accepted.

**Earlier conclusion in this section was wrong — corrected below, not deleted, since the wrong turn and why it was wrong is itself the useful record.** Pushed back on directly ("this doesn't make sense, you're not trying hard enough — check csvql or use mmap") rather than accepted. The reversal above was real as MEASURED, but the measurement compared the wrong things: `scan_array()`/`scanArray()` used a SINGLE-THREADED C ABI path (`scanio_collect()`), and — a genuinely surprising find — `parallelScan()`/`parallelCountRows()` (M9's own multi-threaded engine, already built and tested this session) were **never exposed via the C ABI at all**, not even re-exported from `root.zig`. Every "libscanio under concurrency" number measured before this point was N separate single-threaded processes, never libscanio's actual multi-threaded capability.

Checked csvql's own `src/parallel_mmap.zig` for prior art, as asked: its default parallel mode (`use_parallel_output`, whenever there's no LIMIT/DISTINCT) doesn't build an intermediate array of row objects at all — each worker thread serializes matching rows DIRECTLY into its own growing output buffer (CSV or JSON text), concatenated at the end. Its mmap path (reserved for LIMIT/DISTINCT, which need global ordering across workers) builds rows as zero-copy slices straight into the mapped file — no field-byte duplication at all for that case. Two real, different lessons taken from this: (1) avoid materializing an intermediate representation you're just going to serialize anyway; (2) zero-copy field references are possible when the source data outlives the result (mmap), not possible for libscanio's own chunked-read model without a matching architecture change (not attempted here — mmap reintroduces the exact peak-RSS-tracks-file-size problem M1 exists to avoid, a real tradeoff, not free).

**Fixes made, in order:**

1. **Re-exported `parallelScan`/`ScannedRows` from `root.zig`** — the actual gap that made the C ABI unable to reach them at all.
2. **New C ABI: `scanio_collect_columnar()` / `scanio_parallel_collect_columnar()`** — a genuinely different, more compact wire format than `scanio_collect()`'s NUL-separated row-major buffer: per-column `(data, offsets)` pairs, `offsets` a `u32` array — exactly Apache Arrow's own `StringArray` layout, on purpose. `scanio_parallel_collect_columnar()` drives `parallelScan()` (the real multi-threaded engine) instead of a single-threaded `Ctx`/`Query` loop, then repacks its `OwnedRow` results into the same columnar shape — one, not two, downstream unpacking code paths regardless of which collector produced the result. Verified: exact-match correctness against the single-threaded `scanio_collect_columnar()` on a real multi-thread-boundary-crossing fixture (row SET equality, since worker-range concatenation order isn't guaranteed to match the single-threaded scan order — a real, intentional, documented difference, not a bug).
3. **Real numbers, pure Zig, no Python/Node at all** (`parallel_bench`'s new `materialize` mode, real 50MB slice, same real e-commerce fixture): N=1 **0.05s/15.0MB**, N=8 **0.20s/378.4MB**, N=16 **0.25s/735.8MB**, N=32 **0.47s/894.8MB** — beats DuckDB (0.66-0.89s/75.2-1758.7MB across the same N range) decisively on BOTH axes at every N, even with heavy thread oversubscription (32 processes × ~12 threads = 384 threads on 12 real cores). This is the number that should have been measured before concluding libscanio "loses" on materialized scans — it doesn't; the earlier binding did.
4. **Wired into Python's `scan_array()`** (default path whenever `columns`/`limit` are both unset — those two still fall back to the single-threaded path, since `parallelScan()` doesn't support projection or a row limit yet, a real current scope gap, not silently wrong). Real, measured win: 0.67s/154.3MB → **0.33s/145.2MB** for a single call. Under N-way concurrency, though, a SECOND bottleneck surfaced, distinct from the collection engine: isolated by comparing a raw C ABI call (no Python object construction) against the full `scan_array()` wrapper at the same N — the raw call scales gracefully (threads=2 and threads=0/auto within noise of each other, ~2.8-2.9s at N=32, no blowup), but the full wrapper hit 7.77s at N=32. The gap is Python's OWN list/tuple construction step (`decode()` + `zip()` + list-building) running after the fast C call returns — pure Python bytecode, and it scales badly under N-way concurrent memory pressure, same mechanism as the `scan_bench.zig` finding above, now cleanly isolated to a specific, smaller piece of code instead of "materialization in general."
5. **Built `scan_table()`** — a `pyarrow.Table` constructed via `pa.Array.from_buffers()` directly over the SAME `(data, offsets)` buffers, zero Python object construction per cell at all (only lightweight Arrow array wrappers). Real lifetime-management subtlety, handled explicitly: the C-side buffer must outlive every Arrow object referencing it, but must eventually get freed — solved with `pa.foreign_buffer(address, size, base=handle)`, where `handle` is a small Python object wrapping the raw `cc` pointer with a `__del__` that calls `scanio_collect_columnar_close()`; pyarrow's own refcounting on `base=` (not this function returning) decides when that actually runs. Verified: correct data end-to-end (every value, first row, last row, unique-value check), and no crash/dangling-pointer access after explicit `del` + `gc.collect()`. Real numbers: single-call 145.2MB → **99.1MB** (32% less); under N-way concurrency, dramatically better than `scan_array()`'s blowup (N=32: **3.26s/3246.0MB** vs `scan_array()`'s 7.77s/2590.1MB) — but still behind the pure-Zig number (0.47s/894.8MB) and DuckDB's own N=32 (0.89s/1758.7MB). That remaining gap is Python interpreter/process-startup overhead itself under N-way concurrent process load, not anything in libscanio's Zig core — a real, separate, currently-open cost.
6. **Wired into Node's `scanArray()`** too, same shape (default path when no `columns`, delegates to `scanio_parallel_collect_columnar()`, decodes per-column buffers via `Buffer.toString('utf8', start, end)` instead of one big NUL-split). Real, measured win: 0.69s/171.3MB → **0.43s/173.8MB** for a single call (~38% faster, memory roughly unchanged — same "V8's own object overhead dominates memory, not the collection engine" story as Python). Under concurrency: N=32 **4.00s/4330.2MB**, same class of remaining gap as Python's `scan_array()` (pre-`scan_table()`-fix) — no Node equivalent to `scan_table()`'s Arrow zero-copy path built yet (a real, logged next step, not attempted here).

All of this verified against the full test suite at every step (78 Zig, 52 Python, 53 Node, 10 C-ABI smoke), not just the new-feature-specific checks.

**Sharper thesis, corrected**: the earlier "prefer streaming over bulk-materialize" recommendation was too broad — it was true for the SPECIFIC bindings measured at the time (which weren't using libscanio's actual multi-threaded engine), not a fundamental property of materialization itself. libscanio's real multi-threaded materialized scan is genuinely competitive with or better than DuckDB. The honest, narrower, still-open gap is entirely in the LANGUAGE BINDING layer under high concurrent PROCESS load (Python/Node interpreter overhead, object-model cost, process-startup cost) — not in libscanio's Zig core, which the pure-`parallel_bench` numbers prove out cleanly. Closing that fully would mean either a Node-side Arrow/typed-array equivalent to `scan_table()`, or accepting that very-high-N concurrent SEPARATE PROCESSES (as opposed to one process serving many agent requests, e.g. via a long-lived MCP server process) will always carry real interpreter-startup cost no matter how fast the underlying engine is.

**MCP's own `scan` tool was checked separately and had the exact same gap** — it called `list(libscanio.scan(...))` (the single-threaded row-at-a-time generator, drained in a Python loop), never `scan_array()`, so none of the fixes above actually reached the tool an agent calling this MCP server would use. Fixed: `scan` now calls `scan_array(..., as_dict=True)` directly, which gets the same automatic dispatch (parallel engine when `columns`/`limit` are unset, single-threaded fallback otherwise) for free. Verified through the real `call_tool()` path, both branches: unfiltered-columns call (124,095 correct rows, real 50MB fixture) and the `columns`+`limit` fallback call (3 correctly-projected rows). `README.md`'s "Status" line was also stale — hadn't mentioned `order_by`, `describe`, the parallel scan path, or `scan_table()` at all; updated to reflect what's actually shipped.

7. **`scanio_parallel_collect_columnar()`'s remaining double-copy eliminated.** Even after fix 2 above, that C ABI function still called `parallelScan()` (which builds `OwnedRow` — one `allocator.dupe()` call PER FIELD, millions of tiny allocations for a large result) and then manually re-copied every field a second time into the columnar `(data, offsets)` buffers — correctness-first, not the real fix. Built `parallelScanColumnar()` in `parallel.zig`: same worker/range-splitting shape as `parallelScan()`, but each worker writes directly into its own per-worker `ColumnBuf` array (amortized `ArrayList` growth, zero per-field allocation) instead of `OwnedRow`, and `mergeColumnarWorkers()` combines them with one bulk `appendSlice` per worker per column. `scanio_parallel_collect_columnar()` now calls it directly and moves ownership of the result straight into `CollectColumnarCtx` — no re-copy loop left at all. 5 correctness tests added (CSV cross-checked against `parallelScan()`'s own output via id-column set equality, NDJSON and JSON-array cross-checked against `Query.count()`, plus no-predicate and empty-file edge cases) plus a 6th, different in kind: a `CountingAllocator` wrapping `std.testing.allocator` asserts total `alloc()` calls for a 20,000-row/3-column scan stay under `(rows*cols)/10` — a real regression guard, not just a values check, since a future change reintroducing per-field `allocator.dupe()` would still pass every values-only test while silently blowing this bound. All 94 Zig tests, 52 Python, 53 Node re-verified green.

Numbers were re-checked for a real flaw an outside review caught: the first pass compared a 500MB single-call number against a 50MB sweep — not apples-to-apples. Re-measured on the SAME 500MB fixture, pre-fix code built from a `git worktree` at the commit before this fix (`28c1554`) vs current, swap deltas checked clean on both runs (`vm_stat` Swapins delta 8-24 pages, Swapouts 0 — not the memory-pressure contamination that corrupted an earlier experiment in this file):

| | wall | peak RSS |
|---|---|---|
| pre-fix | 0.61-0.72s | 837-862MB |
| post-fix | 0.97-1.02s | 525-557MB |

Memory: ~38% reduction, confirms the earlier N=32 sweep figure was directionally real even though the fixture mismatch made it look more rigorous than it was. Wall time: reported honestly, not just the flattering number — post-fix was actually SLOWER on this single-call same-fixture comparison, the opposite of the N=32 concurrent-sweep result (2.71s vs pre-fix's 3.26s). Not reconciled: plausible that per-worker `ColumnBuf` growth costs more single-threaded reallocation overhead than `OwnedRow`'s one-shot-sized `parallelScan()` output does at N=1, while the memory win still dominates under concurrent load — a real, open, not-yet-investigated question, not smoothed over.

Also checked: whether the `offsets` array (one `u32` append per row per column) is a second copy-elimination opportunity worth chasing. It isn't — 4 bytes/row/column against real string payload bytes, not a measurable cost. Left alone.

| N | wall | combined RSS |
|---|---|---|
| 1 | 0.31s | 101.6MB |
| 2 | 0.33s | 178.4MB |
| 4 | 0.46s | 291.5MB |
| 8 | 0.74s | 679.5MB |
| 16 | 1.36s | 1,315.3MB |
| 32 | 2.71s | 1,968.3MB |

## Honest gaps

Named directly, not left implicit — asked for an outside-eye assessment of the project and these are the real weak points that came out of it, not resolved yet:

- **`scan_bench`'s wall-time degradation under N-way concurrent load was investigated, not left as an open mystery — root-caused to real host memory pressure, with a real methodological caveat attached.** `vm_stat` before the N=32 run showed only ~68MB genuinely free on the test machine (a live interactive dev box — multiple Claude Code sessions, VS Code, Safari, Teams, Brave, WhatsApp all running, not a dedicated/idle benchmark host); during the run, real swap activity occurred (+44,972 pages swapped in, +279,624 swapped out, ~4.4GB moved through swap). Cross-checked at 1/10th the data (a 50MB slice instead of 500MB): the SAME degradation pattern appeared at a smaller absolute scale (0.11s → 0.51s, ~4.6x, vs 500MB's 0.82s → 4.90s, ~6x) — ruling out "one bad run" and confirming the pattern is real, not noise. Mechanism: materializing every matching row means real memory demand that scales with (matches × N) — DuckDB's streaming-to-`/dev/null` write keeps its per-process footprint small and flat regardless of N, so it stays largely insulated from host memory pressure; libscanio's per-process footprint grows with N, so it's directly exposed to whatever memory pressure exists (from the N siblings themselves, or from everything else running on a shared/busy host). This is the SAME root cause as the memory-total finding, not a separate mystery — and it's consistent with, and mechanistically explains, why `scan_array()`/`scanArray()` are already documented elsewhere in this codebase as "not memory-bounded." Honest caveat: this experiment ran on a busy interactive machine, not a dedicated/idle host — the absolute numbers would likely look better on a quieter box, but the underlying mechanism (bulk materialization's memory cost determines its exposure to memory pressure, streaming architectures are more insulated from it) would hold regardless of host business, since it follows from what each engine actually does, not from this specific run's noise.
- **No fuzzing anywhere.** Every correctness check in this project so far is either a hand-written unit test or a comparison against a real fixture — none of it is property-based/fuzz testing (random/adversarial CSV, NDJSON, or JSON-array input hunting for parser crashes, infinite loops, or silent wrong answers). Given how much of this codebase is hand-written state machines over byte streams (CSV/NDJSON/JSON-array boundary tracking, parallel range-splitting), this is the highest-value gap to close next.
- **JSON-array parallel path is under-tested on shape diversity.** Every real-scale benchmark and correctness check for `parallel.zig`'s JSON-array code ran against fixtures with zero escaped characters (no `\"`, no `\\`) — the escape-handling branches (`skip_next_byte` carrying an in-progress escape across a chunk boundary, `isEscapedAt`-based backslash-parity checks) are covered by small unit tests but never exercised at real multi-GB scale or under real-world messy data (user-generated text fields with actual escapes, unicode, etc.).
- **Windows Node support is a known, unresolved crash.** `scanio_open()` crashes through koffi on Windows (see M5b) — diagnosed as far as pinpointing the exact call, two theories tried and disproven, no working fix. CI skips it rather than papering over it, but it's still an open gap for any Windows Node consumer.
- **DuckDB/xan/qsv/csvql comparisons are spot checks, not a systematic differential test suite.** Real and valuable (caught 3 real bugs this session — the O(n²) header lookup, an NDJSON off-by-one, a dangling-slice UAF), but they're one-off comparisons on 2-3 real fixtures, run manually, not an automated property-based or randomized differential testing harness (generate N random queries, run against both engines, diff results) that would catch classes of bugs the manually-chosen comparisons happen not to exercise.
- **Benchmark numbers aren't statistically rigorous.** Most measurements in this file are 2-4 manual runs on one machine, reported as a range or a single representative number — real, not fabricated, but not the same rigor as multiple hardware configs, formal variance/confidence intervals, or an automated benchmark-tracking harness that would catch regressions over time.

## Path to a publishable evaluation contribution

Outside-eye grading of this project as CS research landed at C+: solid engineering, well-understood techniques, no new algorithm or data structure — not a systems-novelty paper. The realistic path to a defensible A-/B+ isn't inventing new CS, it's turning the per-query-memory-under-concurrency angle (see the N-way concurrency experiment section above) into a rigorous evaluation/methodology contribution — the kind DaMoN and similar venues credit even without a new algorithm. Concrete, ordered work, not done yet:

1. **Name and formally define the metric.** "Peak combined RSS under N-way concurrent load" isn't an established, citable benchmark dimension — every existing CSV/DB benchmark (including this project's own `docs/BENCHMARKS.md`) measures one query at a time. Write a precise definition and state exactly what single-query peak-RSS benchmarks structurally miss.
2. **Reuse established tools instead of bespoke ones — don't build what already exists (DRY/YAGNI applies to research infrastructure too).** Every number so far came from a hand-rolled `ps -o rss=` polling loop and `date +%s.%N` timing. Two of the three pieces this needs are already solved by tools this project doesn't currently depend on: **[hyperfine](https://github.com/sharkdp/hyperfine)** (cross-platform, warmup runs, mean/stddev/min/max, JSON export — the standard tool for exactly this class of CLI wall-time comparison) for single-process timing, paired with the OS's own `/usr/bin/time -l` (macOS) / `-v` (Linux) for single-process peak RSS instead of a custom polling loop. Neither of those covers the one genuinely novel piece — peak COMBINED RSS across N concurrent separate processes — because no established tool measures that shape of workload; that part stays a small custom harness, which is itself supporting evidence the metric is under-tooled, not just under-benchmarked. For fuzzing: Zig 0.15 ships a native, coverage-guided fuzzer in-toolchain (`zig build test --fuzz`, `std.testing.fuzz`) — use that directly instead of writing or vendoring a bespoke fuzz harness or reaching for AFL++/libFuzzer glue.
3. **csvql's existing fuzz/benchmark suite is not reusable here, and shouldn't be forced to be.** It's built around SQL query-plan differential testing — a different problem shape (plans/optimizer) than libscanio's scan/filter primitives. Noted explicitly so this isn't re-litigated later: not an oversight, a scope call.
4. **Explain the mechanism, not just report the number.** Already-collected numbers in the N-way concurrency section above support a sharper, more falsifiable claim than "libscanio uses less memory than DuckDB at N=32": DuckDB's single-process (N=1) memory floor is **86.8MB for a bare filtered count** and **88.6MB for a full materialized COPY of the same filtered rows** (Task 1 vs Task 2 tables above) — nearly identical despite one query touching almost no result data and the other producing 1.24M output rows. If DuckDB's memory scaled with query WORK, these would differ sharply; they don't. That's evidence the ~87MB isn't query cost at all — it's fixed per-process overhead: buffer-pool reservation sized for a single large analytical query, vectorized-execution scaffolding (DataChunk buffers per pipeline operator, `STANDARD_VECTOR_SIZE`-batched even when the batch is nearly empty), and an arena-style allocator that doesn't return freed pages to the OS between queries. Under the N-way experiment's design (N separate DuckDB processes, `PRAGMA threads=1`), that fixed floor multiplies linearly by N because it's PER-PROCESS reservation overhead, not because DuckDB's per-query execution is architecturally memory-unbounded — its own vectorized engine is internally chunked/bounded per operator, which is consistent with why single-query DuckDB wall time stays flat regardless of N. libscanio pays almost none of that floor (no buffer pool, no vectorized operator scaffolding, no schema-sniffing beyond the header row) because it was never built to serve DuckDB's use case — one analyst running one large, complex query — so it never had a reason to reserve memory for that shape of work. Reframed claim, falsifiable and generalizable past this one engine pair: **general-purpose analytical engines carry fixed per-process resource-reservation overhead that's invisible in single-query benchmarks (because it's a constant, not a function of query cost) but multiplies linearly under N-way concurrent PROCESS load** — a structural cost of being built for one-analyst-one-big-query, not a bug, and not a cost a special-purpose scanner needs to pay. Testable next step, not yet run: hold query complexity fixed and vary result-set size across a wider range on DuckDB alone — the floor should stay roughly flat until some cardinality where per-row vector buffers finally dominate, and finding that crossover point would make this a real, falsifiable, checked claim instead of a two-data-point inference.
5. **Release the harness + fixtures as a reusable artifact**, not numbers trapped in this markdown file. Artifact-track credit is real at DaMoN-adjacent venues and is cheaper to earn than a new algorithm.
6. **Close the credibility-undermining gaps first**, specifically the ones already logged below under Honest gaps: no fuzzing anywhere, JSON-array escape-path under-tested at scale, benchmark numbers not statistically rigorous. A rigor-focused paper submitted while those are still open contradicts its own thesis.

Not started except the mechanism reasoning above (item 4), which is real analysis, not a placeholder. Real remaining cost: weeks of evaluation-design and multi-host measurement work, not a quick follow-up — logged here so it isn't lost, not because it's imminent.

## Relationship to csvql

csvql should eventually sit on top of libscanio (SQL parser/planner → libscanio → CSV/NDJSON) rather than duplicating scan logic. Extraction happens gradually, one primitive at a time, each step gated by csvql's existing correctness/fuzz/benchmark suite so it's provably zero-behavior-change before the next step starts. csvql remains the SQL product; libscanio is the reusable engine underneath it.
